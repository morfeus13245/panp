{% extends 'base.html.twig' %}



{% block body %}

<div style="font-size:50px;">{{nombrecliente}} - {{lugar}}</div>

<table class="table table-striped table-inverse table-responsive">
    
        <tbody>
            <tr>                          
                <td><a href="{{ path('clientepedido', {'id':idcliente} ) }}" class="btn btn-block btn-success" style="font-size:60px;">CrearPedido</a></td>
                       
            </tr>
            <tr>               
                <td><a href="{{ path('pedidoscliente',{'idcliente':idcliente}) }}" class="btn btn-block btn-info" style="font-size:60px;">VerPedidos</a></td>
            </tr>
        </tbody>
</table>
        
{% endblock %}

